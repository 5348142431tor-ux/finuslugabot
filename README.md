# FinuslugaBot (math-bot)

Telegram-бот, который принимает сообщения (формулы, чеки, вопросы), считает сумму операций и синхронизирует их с Google Sheets. После обновления **1.1.0** бот умеет отдавать «живые» диалоги в операторскую супергруппу: любой текст без суммы автоматически пересылается в отдельную тему, а ответы операторов возвращаются пользователю от имени бота.

## Возможности
- `/start` регистрирует чат в таблице и показывает подсказку.
- `/сумма` — выводит ненулевые итоги по валютам USD/USDT/RUB/EUR/TRY.
- `/курс` — читает столбцы `rate` из строки текущего чата.
- `/sum` (англ.) и арифметические выражения — записывают результаты в колонку соответствующей валюты.
- `=...` — можно передать готовый результат (бот возьмёт число после `=`).
- OCR чеков (фото/PDF). В группах нужно упоминать бота в подписи.
- Любой текст без цифр → пересылка в операторский форум-чат, создание темы «Имя (ID)» и обратные ответы.

## Быстрый старт
1. Склонируй репозиторий и перейди в каталог `FinuslugaBot`.
2. Установи зависимости: `pip install -r requirements.txt` (Python 3.11+).
3. Скопируй `.env.example` в `.env`, пропиши:
   - `TELEGRAM_TOKEN`
   - `SPREADSHEET_KEY`, `WORKSHEET_NAME`
   - `SUPPORT_CHAT_ID` (forum supergroup ID)
   - путь к Google Service Account JSON (по умолчанию `creds/service-account.json`).
4. Помести JSON сервисного аккаунта в `creds/service-account.json` и выдай доступ к Google Sheet.
5. Запусти бота: `python bot.py` или через Docker `docker compose up --build -d`.

## Конфигурация
- `SUPPORT_CHAT_ID` — супергруппа с включёнными темами, куда бот будет слать обращения клиентов.
- `SUPPORT_TOPICS_FILE` — файл с привязками `user_id → thread_id` (по умолчанию `support_topics.json`, в репозитории игнорируется).
- `RECEIPTS_DIR` — куда сохранять входящие чеки/фото для диагностики.
- `SETTINGS_WORKSHEET` — лист с настройками (по умолчанию `Настройка`, структура: колонка A = ключ, колонка B = значение).

## Вкладка "Курсы"

Чтобы обновить лист с котировками (пример JSON выше), используй скрипт:

```bash
python scripts/update_rates.py path/to/rates.json
```

Скрипт читает `GOOGLE_APPLICATION_CREDENTIALS`/`SPREADSHEET_KEY`, создаёт вкладку `Курсы` (можно переопределить через `RATES_WORKSHEET`) и записывает пары в формате `PAIR покупка/продажа`.


## История версий

### 1.1.1 — 23.03.2026
- Команды в операторских тредах работают как у клиента: `/сумма`, `/kurs`, кнопки из `/menu` обращаются к строке клиента и отвечают прямо в теме.
- Добавлен `/menu` с кнопкой «курс».
- Добавлен `/kosh` (`/кош`) — читает значение `wallet` с листа `Настройка`; сеть отправляется отдельным сообщением.
- Сообщения с цифрами без `/` считаются обычным текстом и пересылаются оператору.
- Исправления: импорт клавиатуры, корректные переводы строк, отзыв оповещений в тредах.

### 1.1.0 — 23.03.2026
- Добавлена маршрутизация вопросов в операторскую супергруппу с отдельными темами на пользователя, ответы из группы возвращаются клиенту от имени бота.
- Любой клиент автоматически заносится в таблицу при `/start` или любом сообщении (включая вопросы без сумм).
- Обновлён README, Docker/Docker Compose и `.env.example` под Python-версию.

### 1.0.6 — 21.03.2026
- После каждой успешной записи бот автоматически отправляет `/сумма` (видно обновлённые остатки без дополнительных команд).

### 1.0.5 — 21.03.2026
- `/сумма` показывает только те валюты, где есть движение (никаких нулевых строк).
- Добавлена поддержка TRY: алиасы (try, lira, лира, tl, лир) и новая колонка `result_TRY` в Google Sheets, плюс OCR распознаёт лиры по символу ₺.

### 1.0.4 — 21.03.2026
- В math-bot теперь нельзя отправлять выражение без валюты: бот просит указать USD/USDT/RUB/EUR и ничего не портит в таблице.
- Добавлены ретраи на чтение Google Sheets (если API рвёт соединение, бот повторяет запрос и продолжает работу).

### 1.0.3 — 21.03.2026
- Команда `/курс` читает все столбцы, где в названии есть `rate`, и отдаёт значения прямо из строки чата в Google Sheets.
- Репозиторий теперь хранит инструкции для работы с курсами в README, чтобы команда понимала, как готовить данные (формулы, ручной ввод и т.д.).

### 1.0.2 — 20.03.2026
- Добавлен OCR-процессинг чеков в math-bot: EasyOCR вытаскивает сумму/валюту и автоматически списывает её из нужной колонки Google Sheets.
- Починили добавление новых чатов: структура листа фиксируется (chat_id строго в колонке A, result_USD/USDT/RUB/EUR — по порядку), поэтому строки больше не "уезжают".
- Улучшена устойчивость обработчиков — бот игнорирует апдейты без текста и аккуратно пересоздаёт отсутствующие валютные колонки.

### 1.0.1 — 20.03.2026
- Обновили README: описаны доступные команды (/start, /help, /ping, эхо) и базовый сценарий запуска.
- Синхронизировали package.json и package-lock.json, чтобы npm install поднимал ровно тот же набор зависимостей.
